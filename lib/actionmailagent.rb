# frozen_string_literal: true

# The gem is `actionmailagent`; the code lives under `action_mail_agent`. This
# is the file Bundler requires when it sees `gem "actionmailagent"` — the same
# shim Rails ships for actionmailbox, and actionagent ships for itself.
require "action_mail_agent"
